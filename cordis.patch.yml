# Web-Agent must not register the shared browser stack itself.
# dsh-builtin-browser owns the `browser`, `browser-electron`, and
# `tool-browser` loader IDs. Web-Agent only contributes its own bundle.
- insert:
    - id: web-agent
      name: dsh-web-agent
