# Web-Agent owns its browser composition. No dsh-builtin-browser or
# dsh-better-sidebar runtime is required. The browser seam and the visible
# Electron workspace provider are both implemented by this plugin.
- insert:
    - id: web-agent-browser
      name: dsh-web-agent/browser
    - id: web-agent
      name: dsh-web-agent
